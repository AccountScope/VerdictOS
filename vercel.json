{
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "regions": ["lhr1"],
  "env": {
    "NEXT_PUBLIC_SUPABASE_URL": "@verdictos-supabase-url",
    "SUPABASE_SERVICE_ROLE_KEY": "@verdictos-service-key",
    "VERDICTOS_API_SECRET": "@verdictos-api-secret",
    "WEBHOOK_SIGNING_SECRET": "@verdictos-webhook-secret"
  }
}
